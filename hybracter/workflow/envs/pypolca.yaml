name: pypolca
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - samtools >=1.18
    - bwa >=0.7.17
    - freebayes >=1.3.1,<1.3.7
    - biopython >=1.76
    - pip
    - pip:
        - pypolca >=0.1.1
